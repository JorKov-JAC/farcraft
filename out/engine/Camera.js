import { Panel } from "./ui.js";
export default class Camera extends Panel {
    pos;
    maxLen;
    constructor(pos, maxLen) {
        this.pos = pos.slice();
    }
}
//# sourceMappingURL=Camera.js.map